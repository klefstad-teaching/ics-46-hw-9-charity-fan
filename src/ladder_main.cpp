#include "ladder.h"

int main() {
    verify_word_ladder();
    
    // set<string> word_list;
    // load_words(word_list, "src/words.txt");

    // vector<string> test_ladder = generate_word_ladder("cat", "dog", word_list);
    // print_word_ladder(test_ladder);

    return 0;
}

// g++ -std=c++20 src/ladder.cpp src/ladder_main.cpp -o l_main
